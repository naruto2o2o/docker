
    server {
        listen       80;
        listen       443;
        server_name  app.dev.com;
        root   /work/aplum_www/web;
        index  index.php index.html;
        access_log  /work/logs/aplum_access.log  main;
        ssl_certificate cert/aplum.com.pem;
        ssl_certificate_key cert/aplum.com.key;

        location / {
            include "denyips.conf";
            try_files $uri $uri/ /index.php?$args;
        }

        location ~ \.php$ {
            fastcgi_pass   172.16.13.129:9000;
            fastcgi_index  index.php;
            fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
            include        fastcgi_params;
            fastcgi_read_timeout 180;
            try_files      $uri =404;
        }

        location ~ /\.ht {
            deny  all;
        }
    }

